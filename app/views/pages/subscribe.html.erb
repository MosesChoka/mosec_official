<div class="flex flex-col mb-20">
  <h1 class="font-bold text-2xl sm:text-3xl text-center mt-20">GET INTERESTING NEWS ABOUT BUSINESS AND TECH!</h1>
  <div class="mt-10">
    <p class="text-center">Hello friend, kindly signup and subscribe to our newsletter to get up to date news on business and technology straight to your email. As we keep updating.</p>
  </div>
  <div class="mx-auto mt-10">
    <% if user_signed_in? %>
      <%= render "layouts/components/manage_newsletter", name: "email_blast", user: current_user %>
    <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete}, class:"text-sm ml-6 pl-1" %>
    <% else %>
      <butt class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded"><%= link_to "Sign In", new_user_session_path %></butt>
    <% end %>
  </div>
  <%#= button_to "Email_Users", email_blast_path(name: "email_blast"), method: :post, data: { turbo_method: :post} %>
</div>
